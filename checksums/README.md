# Input checksums

`fetch_sra.sh` writes `fastq.sha256`. Add hashes for every reference, interval,
population, PoN, truth, and cache manifest before analysis. Large inputs and their
hash manifests are run-specific and therefore not pre-populated here.

